#include <stdio.h>

/**
 * main function - prints "Programming is like building a multilingual puzzle"
 * Returns 0; if exited properly - non-zero incase of error
 */

/* main function */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle"\n);
	/* returns 0 */
	return (0);
}
